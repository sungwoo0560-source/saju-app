# 캐시·차단 조사 보고서 (manse.py)

## 1. 캐시 (Cache)

### ✅ 사용 중인 캐시
| 항목 | 용도 | 비고 |
|------|------|------|
| **saju_ai_cache.json** | 로컬 서술( build_rich_narrative ) 결과 저장 | get_cached_ai_interpretation 에서 캐시 히트 시 즉시 반환, 미스 시 생성 후 set_ai_cache 저장. **유지 권장** |
| **@st.cache_data** (다수) | Streamlit 메모리 캐시 (절기·대운·세운 등 계산) | 재계산 감소용. **유지** |
| **history_memory.json** | SajuMemory – 상담 맥락 유지 | MEMORY_FILE 로 사용. **유지** |
| **saju_save.json** | 즐겨찾기·입력값 복원 | load_from_save / update_favorites 등에서 사용. **유지** |
| **saju_feedback.json** | 맞음/안맞음 피드백 원본 | **유지** |
| **saju_patterns.json** | 피드백 기반 학습 패턴 | **유지** |
| **saju_retention.json** / **saju_user_profile.json** | 사용자 프로필·리텐션 | 코드 내 경로 사용. **유지** |
| **usage_stats.json** | 사용 통계 | **유지** |
| **kasi_24terms.json** | 24절기 데이터 | **유지** |

### ❌ 사용되지 않는 캐시 (쓸데없는 것)
| 항목 | 설명 |
|------|------|
| **saju_cache.json** | `get_saju_cache` / `set_saju_cache` 로 읽고 쓰는 **Brain 1 계산 결과** 캐시인데, **두 함수를 호출하는 코드가 없음**. 즉, 이 캐시는 현재 **전혀 사용되지 않음**. |
| **clear_ai_cache_for_key** | 특정 사주 키의 AI 캐시 무효화 함수이지만 **호출하는 곳이 없음**. |

---

## 2. 차단·필터 (Block / Filter)

### ✅ 실제로 쓰이는 규칙/필터
- **문구 내 “금지” 표현**  
  예: “보증 절대 금지”, “동업 금지” 등 → **내용상 안내용**이라 그대로 두면 됨.
- **미성년자 필터**  
  `filter_for_minor` 등 20세 미만 필터링 → **유지**.

### ❌ 사용되지 않는 차단/필터 (쓸데없는 것)
| 항목 | 설명 |
|------|------|
| **_AI_FORBIDDEN_PHRASES** | AI 출력에서 “다시 계산”, “생년월일 기준으로 계산” 등 **금지 문구 목록**. |
| **validate_ai_output(text)** | 위 금지 문구가 들어간 **줄을 제거**하는 함수. **호출하는 코드 없음** (과거 API 응답 검증용). |
| **SajuJudgmentRules.apply_all(text)** | 단정 완화·부정 균형·과잉 긍정 등 **여러 규칙을 한 번에 적용**. **호출하는 코드 없음** (과거 AI 출력 후처리용). |
| **apply_mansin_filter(text)** | “AI로서 말씀드리면” → “만신의 눈으로 보건대” 등 **말투 치환**. **호출하는 코드 없음**. |
| **SajuExpertPrompt.build_system_prompt** | AI용 시스템 프롬프트 생성. **호출하는 코드 없음** (API 제거 후 미사용). |

---

## 3. 정리

- **쓸데없는 캐시**  
  - **saju_cache.json** 관련: `get_saju_cache` / `set_saju_cache` 가 **호출되지 않음** → 이 파일은 현재 구조에서는 사용되지 않는 캐시.
- **쓸데없는 차단/필터**  
  - **validate_ai_output**, **_AI_FORBIDDEN_PHRASES**, **SajuJudgmentRules.apply_all**, **apply_mansin_filter**, **SajuExpertPrompt.build_system_prompt** 는 모두 **호출처가 없어** 현재는 동작하지 않는 코드.

원하시면 위 “사용 안 함” 항목에 대해  
- 주석으로 “현재 미사용” 표시만 추가하거나  
- 호출부를 복구해 다시 쓰거나  
- 아예 삭제하는 패치를 구체적으로 제안해 드리겠습니다.
