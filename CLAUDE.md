모든 출력(중간 진행 메시지 포함)은 한국어. 일본어·중국어 문장 금지.

## 언어 규칙 (최우선·항상 적용)
- 모든 출력·설명·사고·주석·커밋 메시지는 100% 한국어.
- ★일본어 절대 금지 — "確認します" "実行" "対応" 같은 문장·조사·정형구 전부 금지.
- 한자(사주 명리 용어)는 단어로만 쓰고 문장은 한국어.
  예: "계산 로직 불변을 확인합니다"(O) / "計算ロジック不変を確認します"(X)
- 이 규칙은 매 세션·매 응답에 우선한다.

## 작업 규칙
- 결과 화면·계산 경로에서 in_* 위젯 키 직접 읽기 금지. 명식 입력값은
  frozen 키·_submitted_hour·pils[0]을 사용한다. 명식과 무관한 서술값
  (결혼상태·직업 등)은 on_change로 frozen 값에 동기화한다.
  (근거: R6 계열 버그 — 결과 화면이 제출 시점 스냅샷이 아니라 위젯의
  라이브 값을 다시 읽어 명식·대운 계산 기준 시각이 어긋나던 문제.
  check.py의 in_* 직접 읽기 WARN 검사로 재발 여부를 점검한다.)
- git commit/push는 형의 명시적 지시가 있을 때만 실행한다.

## 작업 순서 규칙 (PLANNER-EXECUTOR)
- PLANNER 명령에 "진단만 하고 멈춤" 또는 "판정 후 대기"가 있으면
  진단 결과만 출력하고 반드시 정지한다. 설계·수정·파일생성·커밋으로 앞서가지 않는다.
- "수정 금지"가 명시된 명령에서는 grep/view/py_compile 같은 읽기 전용만 수행한다.
- 커밋은 PLANNER가 명시적으로 지시할 때만. push는 별도 지시가 있을 때만.
- 여러 단계가 한 명령에 있어도, 각 단계에 "멈춤" 지점이 있으면 거기서 정지한다.

## 쪽집게 룰 외부화 규칙
- rules/ 폴더는 서술 콘텐츠(문구) 전용. 판정 로직(_PAT_SS4 같은 매칭 테이블)은
  rules/에 넣지 않는다 — 조건 데이터화 시 별도 파일(rules/conditions.json)로.
- rules 로더 변수는 반드시 언더스코어 없는 공개명. `from saju_data import *`가
  언더스코어 시작 이름을 제외하므로 manse.py에서 NameError 발생.
- 룰 문구를 코드에 f-string으로 박지 않는다. JSON에서 읽어 로더로 주입.
- 템플릿 변수 치환 시 None이 "None" 문자열로 화면에 나가지 않게 방어한다.

## baseline 갱신 규칙
콘텐츠(문구·판정·수치) 출력이 바뀌는 커밋에는 `tests/apptest_33.py --dump`로 baseline.json 재생성을 포함한다. 재생성 전 diff를 확인해 그 커밋이 만든 변경만 담겼는지 검증하고, 설명되지 않는 변경이 있으면 커밋하지 말고 보고한다. 순수 리팩터(동작·판정 무변경)와 육효 전용 커밋은 재생성 대상이 아니다. 육효 메뉴는 apptest_33 MENU_ORDER에 없어 baseline diff에 잡히지 않는다(별도 커버리지 필요 시 별도 논의).

### apptest 검증 절차(R8-5·R8-6)
- **검증 순서는 항상 ①→②→③**: ① 변경 전 `--compare`로 기존 baseline 대비 diff를 먼저 뽑아 "의도한 줄에만 있는지" 확인·보고 → ② `--dump`로 갱신 → ③ 갱신 직후 `--compare` 0건 확인. **②→③만 하고(①을 건너뛰고) "0건"이라고 보고하는 것은 검증이 아니다** — dump는 방금 만든 걸 자기 자신과 비교하는 것이라 항상 0건이 나온다(R8-5에서 실제로 이 실수를 함).
- **--dump/--compare는 항상 동일한 `--freeze-date`로 실행한다.** `freeze_date` 없는(= None) baseline은 커밋 금지 — daily/monthly/money 등 "오늘·지금 이 시간"을 참조하는 서술이 실행 시각마다 달라져 무관한 diff가 섞인다(R8-5에서 실측: freeze_date 없는 baseline과 비교 시 daily 60건 전부 시각차로 DIFF).
- **baseline 재생성 전 `--determinism` 통과를 함께 확인한다**(같은 `--freeze-date`로): `python tests/apptest_33.py --determinism --freeze-date YYYY-MM-DD`. 서로 다른 두 PYTHONHASHSEED(매번 무작위)로 두 번 dump해 비교 — set/frozenset을 순서 없이 join·f-string 조립하는 지점이 있으면 실행마다 표시 순서가 바뀌는데, 단일 프로세스 실행으로는 절대 안 잡히고 이 옵션으로만 드러난다(R8-6, saju_interpreter.py "재물 조합" frozenset join 버그 사례). FAIL 시 sorted()로 표준 순서(십성: 비견·겁재·식신·상관·편재·정재·편관·정관·편인·정인 / 오행: 木火土金水 / 지지: 子~亥)를 적용해 고친다 — 특정 시드 하나로 고정해서 우회하지 않는다.

## 계산 엔진 보호 규칙 (결정론적 계층)
- `saju_engine.py`, `kasi_24terms.json`, `tools/gen_solar_terms_ephem.py`는
  절기·8글자를 만드는 결정론적 계산 계층이다. 이 세 파일은 형의 명시적
  승인 없이 임의로 수정하지 않는다 — 다른 파일의 문구·톤·UI 수정과
  별개로, 이 계층의 변경은 항상 별도 승인 절차를 거친다.
- 승인 없이 가능한 것: 읽기 전용 진단(grep/dump/py_compile), 이 계층을
  가져다 쓰는 다른 모듈(manse.py 등)의 소비 코드 조사.
- 승인이 필요한 것: 이 세 파일의 실제 내용 수정, kasi_24terms.json
  재생성/병합, gen_solar_terms_ephem.py 재실행 결과 반영.
- 근거: 절입시각 1분 단위 정확도가 월주·연주 판정의 기준선이라 실수 시
  파급 범위가 넓고(모든 사용자의 8글자에 영향), 과거 실측 데이터
  손상(2011·2015·2019년 KASI 항목 4건)처럼 원본 자체의 오류를 식별·교정한
  이력이 있어 신중한 변경 절차가 필요하다.
- 이 계층을 수정할 때 랜덤 명식 표본은 유효 60갑자로만 구성한다
  (CG×JJ 임의조합은 실재하지 않는 일주를 섞어 측정값을 왜곡).
- 결함으로 판단하기 전 git log로 해당 파일의 최근 수정 이력을 먼저 확인한다
  (이미 고쳐진 것을 결함으로 오인한 사례가 반복됨).

## 감사·grep 제외 대상
- 코드 감사·grep 시 아카이브/백업 파일(manse_backup.py, manse_before_split.py,
  SajuArchive/)은 제외한다. 이 파일들의 내용은 현재 동작의 근거가 아니다.

## 격국(格局) 취용 기준
- 이 앱의 격국 판정(get_gyeokguk, saju_interpreter.py)은 다음 순서로 격명을 정한다:
  1) 월지 지장간의 정기(正氣)가 비겁(比肩/劫財)이면, 투출 여부와 무관하게
     建祿格/月劫格을 즉시 확정한다(월지가 일간의 건록·제왕 자리라는 뜻이므로).
  2) 그 외에는 "월지 지장간의 글자(정기→중기→여기 순)가 4주 천간에 그대로
     찍혀 있는지"만 본다 — 오행이 같아도 음양(정/편)이 다른 글자는 투출로
     인정하지 않는다(예: 辰월에서 정기 戊가 불투하고 己만 있으면 투출
     불인정).
  3) 중기·여기가 투출했으면 그 십성으로 격명을 정한다(단, 그게 비겁이면
     건너뛰고 다음 후보를 본다 — 비겁은 정식 격의 대상이 아니라는 원칙).
  4) 아무 후보도 투출 안 했으면 정기 기준으로 暗格 처리한다.
- **자평진전(子平眞詮) 엄격 해석과의 차이**: 자평진전은 정기가 불투하면
  중기·여기 투출을 아예 인정하지 않고 정기 하나로만 격을 정하는 경향이
  강하다(暗格 원칙). 이 앱은 중기·여기 투출도 격의 근거로 인정한다(雜格)
  — 이는 의도된 선택이다: 8정격 중 하나로 확정 짓지 못해 "미정격"으로
  빠지는 비율을 줄이고, 실제 천간에 드러난 기운을 격 판정에 반영하기
  위함이다. N=1000 실측 기준 이 차이로 인해 약 32.7%의 명식에서 자평진전
  엄격 해석과 다른 격명이 나온다.
- 오행이 같은 다른 천간(예: 戊 대신 己)을 투출로 인정하는 "B안"은 검토 후
  기각한다: 正/偏 라벨이 24.1%에서 뒤집혀 정재/편재·정관/편관의 십성
  구분이 무너지기 때문이다.
- 이 기준을 바꾸려면 형의 명시적 승인이 필요하다(계산 엔진 보호 규칙과
  같은 절차 — 격국 판정은 용신·직업적성 등 다른 서술의 상류 재료라
  파급 범위가 넓다).

## 개인정보 취급
- 이 앱은 이름·생년월일·전화번호 등 개인정보를 저장하지 않는다.
- DB 파일·상담 로그·개인 식별 데이터를 저장소에 커밋하지 않는다.
- 개인정보를 담는 기능을 새로 만들지 않는다.
