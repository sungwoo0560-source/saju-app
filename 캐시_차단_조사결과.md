# 캐시·차단 조사 결과 (manse.py)

## 1. 캐시

| 항목 | 파일/함수 | 사용 여부 | 비고 |
|------|-----------|-----------|------|
| **Brain 1 캐시** | `saju_cache.json`, `get_saju_cache`, `set_saju_cache`, `create_saju_cache_key` | ❌ **미사용** | 정의만 있고 호출하는 코드 없음. 사주 기둥 계산 결과를 캐싱하려던 구조로 보임. |
| **해석 캐시** | `saju_ai_cache.json`, `get_ai_cache`, `set_ai_cache` | ✅ 사용 중 | 로컬 해석( build_rich_narrative ) 결과 캐싱. 동일 사주 재요청 시 즉시 반환. |
| **캐시 무효화** | `clear_ai_cache_for_key()` | ❌ **미사용** | 정의만 있고 호출처 없음. |
| **메모리 캐시 파일** | `saju_memory_cache.json` | ❌ **미참조** | manse.py에서 참조 없음. 레거시 또는 외부 생성 파일로 추정. |
| **Streamlit 캐시** | `@st.cache_data` (다수 함수) | ✅ 사용 중 | 만세력·절기·대운 등 계산 결과 메모리 캐싱. |

## 2. 차단/필터

| 항목 | 위치 | 사용 여부 | 비고 |
|------|------|-----------|------|
| **_AI_FORBIDDEN_PHRASES** | 10552~10576행 | ❌ **미사용** | AI 출력에서 "계산 침범" 문장 제거용. `validate_ai_output()` 에서만 사용. |
| **validate_ai_output()** | 10580~10598행 | ❌ **미호출** | API 제거 후 호출하는 곳 없음. |
| **apply_mansin_filter()** | 16070~16095행 | ❌ **미호출** | AI/보고서체 → 만신 톤 치환. 호출처 없음. |

## 3. JSON 데이터 파일 사용 현황

| 파일 | 사용 여부 |
|------|-----------|
| `history_memory.json` | ✅ 사용 (상담 맥락) |
| `saju_save.json` | ✅ 사용 (상태 저장/복원, 즐겨찾기) |
| `saju_feedback.json` | ✅ 사용 (피드백 저장) |
| `saju_patterns.json` | ✅ 사용 (패턴 학습 결과) |
| `saju_retention.json` | ✅ 사용 (스트릭/리텐션) |
| `saju_user_profile.json` | ✅ 사용 (사용자 프로필) |
| `usage_stats.json` | ✅ 사용 (일일 제한 등) |
| `kasi_24terms.json` | ✅ 사용 (24절기) |
| `saju_cache.json` | ⚠️ 코드에서 읽/쓰 안 함 (미연결) |
| `saju_ai_cache.json` | ✅ 사용 (로컬 해석 캐시) |
| `saju_memory_cache.json` | ❌ 코드에서 참조 없음 |

## 4. 정리 권장 사항

- **삭제/비활성화 가능**: `validate_ai_output`, `_AI_FORBIDDEN_PHRASES`, `apply_mansin_filter` (현재 미호출).
- **선택**: `get_saju_cache` / `set_saju_cache` 를 실제 사주 계산 경로에 연결해 `saju_cache.json` 활용, 또는 사용 계획 없으면 제거.
- **선택**: `clear_ai_cache_for_key` 호출처 추가(재분석 시 캐시 비우기) 또는 미사용이면 제거.
- **파일**: `saju_memory_cache.json` 은 코드 미참조 시 레거시로 보고 삭제해도 됨.
